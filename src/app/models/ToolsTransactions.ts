export interface ToolsTransactions {
  toolId: number;
  toolName: string;
  statusName: string;
  borrowDate: string;
  userName: string;
  userId: number;
  dueDate: string;
  returnDate: string;
  updatedAt: string;
}

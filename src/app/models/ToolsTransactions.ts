export interface ToolsTransactions {
  toolId: number;
  toolName: string;
  statusName: string;
  borrowDate: string;
  userName: string;
  dueDate: string;
  returnDate: string;
  updatedAt: string;
}

export interface Transaction {
  toolId: number;
  toolName: string;
  statusName: string;
  borrowDate: string;
  userName: string;
  dueDate: string;
  returnDate: string;
  transactionDate: string;
  transactionStatus: string;
}
